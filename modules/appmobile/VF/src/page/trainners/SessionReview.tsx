import { StyleSheet, Text } from "react-native";

const SessionReview = ({ navigation }: any) => {
    return (
        <Text style={styles.container}> hello SessionReview</Text>
    )

}

const styles = StyleSheet.create({
    container: {
        flex: 1
    }
})

export default SessionReview;