import { StyleSheet, Text } from "react-native";

const Schedule = ({ navigation }: any) => {
    return (
        <Text style={styles.container}> hello Schedule</Text>
    )

}

const styles = StyleSheet.create({
    container: {
        flex: 1
    }
})

export default Schedule;